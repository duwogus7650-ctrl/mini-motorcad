# 전면 재점검 — 발견 목록 (2026-06-21)

병렬 코드리뷰 3개(App.jsx 물리 / App.jsx React·UI / femm_server.py) 종합. 총 **58건** + open question 4건.
**중요**: 검증 운전점(400W·1250W, 진각0°)의 수치는 전부 정확. 아래는 off-nominal 견고성 + 코드품질 이슈.
실행검증 전부 PASS(build, verify_400w, validate_engine, verify_autofit, verify_aedt, feedback-runner). verify_fit는 CLI 인자 필요(버그 아님).

심각도: 🔴HIGH 🟠MED 🟡LOW · 확신도 high/med/low · ✓=직접 재확인

---

## A. 물리/수치 엔진 — app/src/App.jsx (17건)

- [ ] **P1 🔴 high ✓** `App.jsx:732` vs `:2797` — 운전점 토크 `1.5·pp·lam·Iq`(정렬항만), T-N·효율맵은 `+(Ld−Lq)·id·iq`(릴럭턴스항). 진각0서 id=0이라 일치, 진각≠0서 갈라짐. → idPk/iqPk(896-897) 끌어올려 공유, 동일 dq식.
- [ ] **P2 🔴 high** `App.jsx:728/869/896` — dq전류 3중복(Iq@728, iqPk@897, iqL@869 / idPk@896, idL@869). 부호규약 드리프트 잠복. → 727부근 1회 계산 후 재사용.
- [ ] **P3 🟠 high** `App.jsx:2728-2730` — BEMF합성 `÷s1=sin(π·alpha/2)`, magnetArcED=0서 NaN. rawRes가드 미적용. → `s1||1e-9` / a≤0 조기클램프.
- [ ] **P4 🟠 med** `App.jsx:700,616-620` — windingAnalysis 도체표 turnsPerCoil만; parallelPaths>1서 FEMM/MMF 암페어턴 ×parallelPaths 오배율 가능. → femmLua slot전류 /parallelPaths 확인.
- [ ] **P5 🟠 high** `App.jsx:694-695` — Carter kc `Math.max`바닥 → 광폭개구서 kc 폭발(물리상한 아님) → Bg→0 조용히. → `kc=min(max(kc,1),3)`.
- [ ] **P6 🟠 med** `App.jsx:825` — RacRdc 두 항 모두 ξ⁴, skin O(ξ)항 누락 → 고fe서 AC손 과소. eff맵 fe²스케일은 내부일관. cAC로 완화. → ξ<~1 유효범위 명시.
- [ ] **P7 🟠 med** `App.jsx:823` — mLayer=round(windingDepth/wireDia) 1로 바닥→근접손 0; round(not ceil). → ceil / condPerSlot 기반.
- [ ] **P8 🟡 high** `App.jsx:2902-2905` — solveLin 피벗 `||1e-12` → 특이 열행렬서 유한 garbage 무경고. → 잔차 ‖G·T−Q‖ 체크.
- [ ] **P9 🟡 high** `App.jsx:883` — Km=torque/√Pcu, torque<0(adv>90°)서 음수. → torque>0 게이트.
- [ ] **P10 🟡 high** `App.jsx:821` — deltaSkin=sqrt(rho/(π·fe·MU0)) fe=0서 Infinity(현재 benign, 잠복). → feSafe=max(fe,1e-6).
- [ ] **P11 🟡 med** `App.jsx:2839` vs `:831` — eff맵 Pfe는 약계자 자속감소(fluxR²) 적용, compute().Pfe는 미적용 → 약계자영역 규약차. → 문서화 or 통일.
- [ ] **P12 🟡 high** `App.jsx:906` — numLam=magneticLength/stl.thk 무가드(테이블 항상 thk有, 잠복). → `||0.5`.
- [ ] **P13 🟡 med** `App.jsx:885-887` — coggingPeriod: poles=0→lcmSP=0→Infinity, finite게이트 미포함 누출. → lcmSP>0 가드 + poleNumber>0 게이트.
- [ ] **P14 🟡 med** `App.jsx:889` — optSkew=coggingPeriod이나 최적스큐는 통상 1슬롯피치(18s16p서 8×차). → 스텝/연속 스큐 라벨 구분.
- [ ] **P15 🟡 low** `App.jsx:469` — reflectOuter R=hypot`||1e-9`; 원점정점→k≈1e11 폭발(외전형만, 미관측). → R<ε면 그대로 통과.
- [ ] **P16 🟡 med** `App.jsx:794` — ByAnalytic `Math.max(byDepth,0.1)`바닥 → 퇴화 요크서 By 폭발 → Pfe∝By² 부풀고 finite게이트 통과. → By≤~2.5T 클램프/경고.
- [ ] **P17 🟡 high** `App.jsx:88,108-116` — DXF파서 CIRCLE/ARC를 cx,cy,r 유한검사 없이 push; NaN cx가 중심중앙값 오염. → 유한 필터.

## B. React/UI — app/src/App.jsx (23건)

- [ ] **U1 🔴 high** `App.jsx:2356`(Tbl),`2369`(r),`2087`(TR),`2968`(ThermalTab Row가 모듈 Row@947 섀도잉) — 렌더본문 내 컴포넌트 정의(매 렌더 재생성). 현재 입력없어 무증상이나 포커스풀림 버그클래스. → 모듈레벨 승격, ThermalTab.Row 개명.
- [ ] **U2 🔴 med** `App.jsx:1179` — NaN가드 5키만 검사; Jrotor/PF/thermal hot,tau/CSV가 "NaN" 표시 가능. → `fmt(v)` 헬퍼(비유한→"—")로 일원화. **(최우선 권장)**
- [ ] **U3 🔴 high** `App.jsx:1932,2090` — Winding/Materials가 !res서 `return null`→빈 화면(타 탭은 안내). → 안내 메시지 폴백.
- [ ] **U4 🟠 high** `App.jsx:1061,1340` — SelfCheckTab `solved` 받지만 무시 → Solve 전 결과 표시(게이팅 불일치). → 게이트 추가 or dead prop 제거.
- [ ] **U5 🟠 high** `App.jsx:962` — NumIn `parseFloat()||0`이 빈값/"-"/"1."→0 강제; 클리어/음수/소수 타이핑 방해. → 로컬 문자열버퍼, 유한시 commit.
- [ ] **U6 🟠 high** `App.jsx:2724` dep[res,calc] — GraphsTab useMemo가 calc 키스트로크마다 T-N+88×60 eff그리드(~430k반복) 재계산. solve게이트로 완화. → 사용 primitive만 의존.
- [ ] **U7 🟠 med** `App.jsx:2650`(EffMap),`2519`(Plot),`2568`(Bars) — 렌더본문서 중작업(5280 rect, marching-squares) 비메모. → useMemo/React.memo.
- [ ] **U8 🟠 med** `App.jsx:1964-1973` — 와이어게이지 `<select>` value=""+index값; 선택후 초기화되는 write-only 피커, 테이블 재정렬에 취약. → aria-label/상태추적.
- [ ] **U9 🟠 med** `App.jsx:1462-1466,1903-1907` — ResizeObserver가 `draw` 정체성 변할 때마다 재구독(deps churn). → 안정 observer + ref-held draw.
- [ ] **U10 🟠 med** `App.jsx:201,224,2522,2570,2595` — `Math.min/max(...arr)` 대형배열 spread → 거대 DXF/그리드서 RangeError 위험. → reduce 기반 min/max.
- [ ] **U11 🟠 low** `App.jsx:1501-1513,1611` — loadFile async가 autoPendingRef 경쟁(경미). → 허용, 인지.
- [ ] **U12 🟠 med** `App.jsx:1176` — compute() 매 키스트로크 실행(디바운스無); slot/poleNumber가 O(Ns)루프+경로빌드 구동, 저사양서 랙 가능. → debounce/useDeferredValue.
- [ ] **U13 🟡 high** `RadialGeometryMatcher.legacy.jsx`(553줄) — 임포트 0, 데드파일. → 삭제.
- [ ] **U14 🟡 high** `App.jsx:1061` — SelfCheckTab `wind` prop 미사용(dead). → 제거.
- [ ] **U15 🟡 med** 다수 — 동적리스트 index-as-key(warnings@1622); 대부분 안정키. → 조치불요.
- [ ] **U16 🟡 high** `App.jsx:1206,1218,1251,1512,...` — native alert()/confirm() 흐름제어; 스레드블록·비스타일·커스텀UI와 불일치. → 토스트/모달(선택).
- [ ] **U17 🟡 low** `App.jsx:70,1644` — location.reload()/confirm()/alert() 암묵 글로벌(window.無). → window. 접두 / eslint.
- [ ] **U18 🟡 med** `App.jsx:2546` — Plot 세로 그리드선 `#F4F6F8`(거의 흰색) vs 가로 `#1a2740`, 다크패널서 튐(복붙 실수). → `#1a2740`로.
- [ ] **U19 🟡 low** `App.jsx:2701`(+1664) — JSX 내 op IIFE 매 렌더. 동작 정상. → 불요.
- [ ] **U20 🟡 med** `App.jsx:75` — ErrorBoundary `err&&err.message||err` 우선순위: 빈 메시지→String(errObj). → `err?.message??err`.
- [ ] **U21 🟡 low** `App.jsx:1381,1822` — getContext("2d") null 미체크. → 선택적 가드.
- [ ] **U22 🟡 med** `App.jsx:1459` vs `:1366` — draw 콜백이 whole geo/layers/dxfT 객체 사용(중첩변경시 재생성); fit memo@1366은 모범(명시 primitive). → U9와 동일.
- [ ] **U23 🟡 high** `app/package.json` — ESLint/eslint-plugin-react-hooks 부재(U1·deps 이슈 근본원인). → eslint + react-hooks + no-unstable-nested-components. **(단일 최대 레버리지)**

## C. Python FEMM 브릿지 — fea/femm_server.py + validation (18건)

- [ ] **F1 🔴 high ✓** `femm_server.py:318-319` — req 12키 검증하나 statorRot/rotorRot/Br/slotArea/Rsh 무조건 접근(100,102,112,222,345-346) → KeyError("필수입력누락" 계약 깨짐). → req 추가 or d.get 기본값.
- [ ] **F2 🔴 med** `femm_server.py:140,511-524` — 에러경로서 mi_close()는 하나 closefemm() 누락 → FEMM 프로세스 누수 누적. → except/finally서 closefemm().
- [ ] **F3 🔴 med ✓** `femm_server.py:436,495` — nLoad=0→avgT ZeroDivisionError; nCog=0→max([]) ValueError. → `max(1,int(...))` 클램프.
- [ ] **F4 🔴 med** `femm_server.py:343,361` — Ns/poles int 미강제; float→range(float) TypeError 깊은 build()서. → 검증시 int() 강제 + >0/짝수.
- [ ] **F5 🟠 high** `femm_server.py:524` — 에러응답에 traceback.format_exc() 노출; CORS 전면개방→trace 수확가능(127.0.0.1 바인드라 저위험). → JSON서 trace 제거, 콘솔만.
- [ ] **F6 🟠 high** `femm_server.py:42,338` — _solve_lock 직렬화 정상; 잔여: 전역 femm 인스턴스가 lock범위 밖(미래 비-/solve 호출자시 경쟁). → 불변식 주석.
- [ ] **F7 🟠 med** `femm_server.py:437` — ripT 가드 1e-6 but torqueValid 1e-3; 근0 토크서 거대 무의미 리플% 직렬화. → 동일 1e-3 / None.
- [ ] **F8 🟠 med** `femm_server.py:511-524` — 에러경로 상태리셋無이나 newdocument(0)이 구문서 폐기→처리됨. 결함아님(F2와 짝). → 변경불요.
- [ ] **F9 🟠 med** `femm_server.py:216` — 하드코딩 'mini_motorcad.fem'→CWD; 동시 인스턴스 충돌, 읽기전용 CWD 실패, .fem/.ans 잔재(정리無). → tempfile + 고유명.
- [ ] **F10 🟠 med** `femm_server.py:347-352` — 선택 param float()/int() try/except無; 비숫자(phaseAdv:"abc")→ValueError(lock 후). → pre-lock try서 강제.
- [ ] **F11 🟠 low** `femm_server.py:437,495` — NaN avgT→abs>1e-6 False→ripT=0 (NaN을 0%로 은폐; torqueValid도 False라 신호有). → `ripT=None if not isfinite`.
- [ ] **F12 🟡 high** `femm_server.py:454,471` — 루프변수 `ia`(각인덱스)가 `ia`(A상전류) 섀도잉; 다른 스코프라 버그아니나 가독성 위험. → 인덱스 개명.
- [ ] **F13 🟡 high** `femm_server.py:14,19` — 미사용 임포트 없음(검증). → 불요.
- [ ] **F14 🟡 med** `femm_server.py:315` — get_json(force=True) + MAX_CONTENT_LENGTH無 → 무제한 본문 버퍼. → `MAX_CONTENT_LENGTH=2MB`.
- [ ] **F15 🟡 med** `femm_server.py:144-148` — smartmesh 실패 캐치+프린트 후 계속 → smartmesh ON이면 ~200k노드 수시간 행. → 못 끄면 명확한 에러.
- [ ] **F16 🟡 high** `femm_server.py:24-28,518,522` — bare except 없음; cleanup이 원본에러 보존(정상). → 불요.
- [ ] **F17 🟡 high** `validation/validate_engine.py:1-35` — pp=poles//2(int) vs 서버 pp=poles/2(float) 교차파일 불일치; __main__ 가드無; Lstk@7 미사용. → Lstk 제거.
- [ ] **F18 🟡 med** `femm_server.py:534-536` — Flask dev서버 단일스레드; solve 중 /ping 블록; 프로덕션용 아님. → threaded=True(lock 보호) or 문서화.

## D. Open Questions (저확신 — 런타임 확인 필요)

- [ ] **OQ1 🔴? low** `App.jsx:2902` — solveLin 특이행렬→thermal "NaN °C". 퇴화입력(stackLength→0, statorLamDia≈statorBore) 런타임 프로브 필요. (P8/U2와 연계)
- [ ] **OQ2 🟠? low** `App.jsx:1331` — ErrorBoundary resetKey가 매 렌더 전체상태 JSON.stringify. 무시할만함.
- [ ] **OQ3 🔴? low** `femm_server.py:140` — openfemm()~opened=True 사이 실패시 도달불가 인스턴스? `opened` 플래그는 dead(미참조). FEMM 설치 런타임 확인. (F2와 연계)
- [ ] **OQ4 🟠? low** `femm_server.py:361,427` — cogPeriod 스텝 range(nLoad)는 엔드포인트 제외=주기평균에 정확(off-by-one 아님 확인). load리플 기계주기 적정성은 FEA 확인.

---

## 잘 되어 있는 점 (유지)
compute() 방어적 0-나눗셈 가드(주석포함), lastResRef stale-badge 패턴, fit memo@1366(명시 primitive deps 모범), FEMM `_finite()` 재귀 NaN→null(잘못된 JSON 차단), _solve_lock+CoInitialize 동시성, ironOk 플래그(0대신 실패신호), 임포트 전 형상가드, ErrorBoundary 도메인 복구UI.

## 집계
🔴HIGH 9 (P1,P2,U1,U2,U3,F1,F2,F3,F4) · 🟠MED 22 · 🟡LOW 27 · OQ 4
